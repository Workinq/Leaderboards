name: ${project.name}
main: ${project.groupId}.${project.artifactId}
version: ${project.version}
author: ${project.author}
depend: [Factions, MassiveCore, HolographicDisplays, PvPLabCaneTop, PvPLabEnvoy, Koth]
commands:
  leaderboards:
    usage: /<command>
    description: Open the leaderboards gui
    aliases: [lb, lbs, leaderboard]